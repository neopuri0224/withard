<% if our_room == true %>
  <%link_to "Talk!",room_path(room_id),class: 'btn btn-success' %>
<% else %>
  <%= form_for room do |f| %>
    <%= fields_for entry do |e| %>
      <%= e.hidden_field :user_id, value: user.id %>
    <% end %>
    <%= f.submit "チャットを始める" %>
  <% end %>
<% end %>